// Empty CommonJS stub used by seed-zd410-test.ts to neutralize the
// `import "server-only"` guard inside src/lib/cup-label/render-zebra-cup.ts
// when that module is loaded from a standalone tsx script. See seed
// script for the _resolveFilename hook that maps "server-only" here.
module.exports = {};
