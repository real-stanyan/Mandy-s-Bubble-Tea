// Project-specific Square-related types live here. Re-exports and
// thin view-model types will be added as features are built (catalog,
// cart, orders, loyalty). The Square SDK's own types should be imported
// directly from "square" (e.g. `import type { Square } from "square"`).

export {};
